# Compiled output
/dist
/coverage
/.angular/cache

# Node
/node_modules

# Local secrets (see CLAUDE.md: never commit real API URLs/credentials)
.env

# Visual Studio Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# System files
.DS_Store
Thumbs.db

# Local env config (see .env.example) — never commit real values.
.env
